import {BaseObject} from '/src/util/baseobject.es6';
import '/src/util.js';

export default class Chart extends BaseObject
{
	constructor()
	{
		super();

		console.dir('hi fucking there!!!');
		Util.extend({}, {});
	}
}